{
  "name": "browservj",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/generate-bundles.mjs",
    "dev": "vite --host 0.0.0.0 --port 9000",
    "prebuild": "node scripts/generate-bundles.mjs",
    "build": "vite build && node scripts/postbuild-copy.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 9000",
    "test:smoke": "playwright test"
  },
  "dependencies": {
    "three": "^0.181.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "esbuild": "^0.25.12",
    "fast-glob": "^3.3.3",
    "vite": "^7.1.12"
  }
}
